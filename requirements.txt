# requirements.txt — sv2 (extracción IA 2 fases)
# Paquete común: pip install -e ../comun (local) / Dockerfile (build)
fastapi>=0.115
uvicorn>=0.30
python-multipart>=0.0.9
pydantic>=2.7
pydantic-settings>=2.2
httpx>=0.27
pyyaml>=6.0
anthropic>=0.40
openai>=1.50
google-genai>=1.0
# Solo si GOOGLE_DOCUMENT_AI_ENABLED=true (cliente gRPC oficial):
google-cloud-documentai>=2.30
